import React from 'react';

const MainTitleSection = ({ title }) => {
  return (
    <h2 className="font-bold uppercase text-3xl text-indigo-700 text-center my-3">
      {title}
    </h2>
  );
};

export default MainTitleSection;
